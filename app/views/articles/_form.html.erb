<%= form_with(model: article) do |form| %>
  <% if article.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
        <% article.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :es_title, "Titulo en español", style: "display: block" %>
    <%= form.text_field :es_title %>
  </div>

  <div>
    <%= form.label :es_body, "Contenido en español", style: "display: block" %>
    <%= form.text_area :es_body %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
